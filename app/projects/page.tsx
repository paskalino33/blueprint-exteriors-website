import Header from '@/components/Header';import Footer from '@/components/Footer';import { projects } from '@/lib/site';
export const metadata={title:'Project Gallery | Blueprint Exteriors LLC',description:'View roofing, siding, deck, window, and exterior renovation projects by Blueprint Exteriors LLC.'}
export default function Projects(){return <><Header/><main><section className="hero"><div className="container"><div className="eyebrow">Project Gallery</div><h1>Recent Exterior Projects</h1><p>Add your real project photos to make this section sell your work.</p></div></section><section className="section"><div className="container"><div className="projects">{projects.map(p=><div className="project" key={p.title}><div className="project-img">Replace with<br/>{p.image}</div><h3>{p.title}</h3><p>{p.category} • {p.location}</p></div>)}</div></div></section></main><Footer/></>}
